# Setup Guide

1. Install Node.js and MongoDB.
2. Create a Supabase project and bucket named `kyc-documents`.
3. Copy environment file:

```bash
cp .env.example .env
```

4. Fill MongoDB, JWT, and Supabase values in `.env`.
5. Install and run:

```bash
npm install
npm run dev
```
